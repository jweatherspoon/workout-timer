const Workout = (name, exercises, onTime, restTime) => {
    return {
        name: name,
        exercises: exercises,
        onTime: onTime,
        restTime: restTime,
    }
}

export default Workout;